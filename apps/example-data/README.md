---
projectTitle: Girk Data
projectDescription: A focused build-time data example for Girk.
---

# Girk Data

This example exists to show build-time data sources without mixing that feature into the broader basic example.

- a list page repeated from local JSON
- generated detail pages from one markdown template
- direct first-load routes because the pages are real static files

Use the generated project pages below to inspect the result.
