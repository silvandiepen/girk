# Girk Config

This example shows a project that keeps its shared settings in `girk.config.json`.

The homepage content stays simple, while the project title, description, logo, colors, and extra stylesheet come from the config file.
